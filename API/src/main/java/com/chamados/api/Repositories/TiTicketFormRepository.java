package com.chamados.api.Repositories;

import com.chamados.api.Tickets.TiTicketForm;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TiTicketFormRepository extends JpaRepository<TiTicketForm, Long> {
}